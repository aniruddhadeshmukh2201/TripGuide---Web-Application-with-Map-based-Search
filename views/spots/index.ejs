<% include ../partials/header%>
<div class="thumbnail" id='cluster-map' style=' height: 250px;'></div>
<div class="container">
     <!-- <header class="jumbotron">
        <div class="container">
            <h1>Welcome to YelpCamp!</h1>
            <p>
                View our hand-picked spots from all over the world
            </p>
            <p>
                <a class="btn btn-primary btn-lg " href="/spots/new">Add new spots</a>
            </p>
        </div>
    </header> -->
    
    <div class="row text-center" style="display:flex; flex-wrap: wrap;">
        <% spots.forEach(function(spot){ console.log(spot) %>
            <div class="col-md-3 col-sm-6">
               <div class="thumbnail">
                    <!-- <img src="<%= spot.images[0].url %>"> -->
                    <%if(spot.images.length) {%>
                        <img class="img-fluid" alt="" src="<%=spot.images[0].url%>">
                        <% }else {%>
                        <img class="img-fluid" alt=""
                            src="">
                        <% } %>
                    <div class="caption">
                        <h4> <%= spot.name%></h4>
                    </div>
                    <p>
                        <a href="/spots/<%= spot._id %>" class="btn btn-primary">More Info</a>
                    </p>
                </div>
               
            </div>
        
        <% })%>
    </div>
</div>

<script>
    const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
    const spots = { features: <%- JSON.stringify(spots) %>}
</script>

<script src="/javascript/clusterMap.js"></script>

<% include ../partials/footer%>